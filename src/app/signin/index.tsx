import Auth from "@/components/Auth";
import React from "react";
import { Text, View } from "react-native";

const Page = () => {
	return (
		<View>
			<Auth />
		</View>
	);
};

export default Page;
